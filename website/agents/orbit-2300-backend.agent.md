## per-server steering: 2300-backend

This file is appended to the shared Orbit steering (`orbit.agent.md`)
for the `@orbit-2300-backend` participant only.

Use it to record steering specific to the 2300-backend Smalltalk image:
- conventions and idioms peculiar to this image
- packages, classes, or workflows the agent should prefer
- known hazards or "do not touch" areas
- preferred test entry points

Edit this file freely; it is read each time the participant is
(re-)created (when the server transitions to running). Stop and start
the 2300-backend server in the Orbit view to force a re-read.
