# macOS
.DS_Store

# Editor backups
*~
*.swp

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs
logs/
*.log

commentary/
audit/

# Keep sync local state
.keep-sync-peer-tokens.json
.keep-sync-machine-id
.keep-sync-gist-id

# Local secrets (e.g. keep-sync GitHub token) — never commit.
secrets/
